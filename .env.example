GEMINI_API_KEY=your_api_key_here

# Your local grocery store. Not yet read by code — reserved for the
# auto-fetch circular feature (see issue #19). Fill these in your local
# .env (which is gitignored) so they're ready when the feature lands.
STORE_NAME=
STORE_ADDRESS=
STORE_ZIP=
STORE_CODE=
